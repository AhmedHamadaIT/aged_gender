# Environments

venv/
.venv/
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
htmlcov/
*.resolved

# IDEs
.vscode/
.cursor/
.scripts/
.github/

requirements-ci.txt
# Local scripts (not tracked)
scripts/

# Specify / Speckit (local tooling & generated specs)
.specify/
specs/
# Local pytest / integration test workspace (entire tree)
test_reports/
pytest.ini
curl_smoke.sh 
# Project Specific
outputs/
# mediamtx binary & local config (download separately)
bin/
mediamtx.yml
mediamtx*.yml
# Runtime storage (created by FrameBus / task workers)
store/
# Test / QA artifacts
test_logs/
# Local export trees (e.g. batch image+json dumps; not under outputs/)
output*/
videos/
logger/app.log
evidence/
torch*

# Local secrets / OS noise
*.pem
.DS_Store
Thumbs.db
*.tmp
*.temp
*.bak
*.swp
*.swo
*~

# Cashier frame batch tests / YOLO tooling (outputs/ already ignores test runs)
runs/
wandb/
*.engine

# YOLO / ONNX weights at repo root (if copied outside models/)
yolov8n.pt
yolov8n.onnx
yolov8n.yaml

# models/ — ship weights to each host / edge separately (not in git)
models/*
!models/.gitkeep

# Ad-hoc test scripts at repo root only (tracked pytest lives under tests/)
/test_*.py
/*_test.py
/test.py
test_results/
test_results.log
README_CASHIER_TESTING.md

# Local QA / scratch markdown (keep API & architecture docs in git)
BUG_REPORT.md
docs/cashier_demo_curl_and_logs.md
docs/cashier_framing_test_results.md
docs/logs.md
docs/SERVICE_TEST.md
docs/test-on-edge.md
