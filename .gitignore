# Environments

venv/
.venv/
# .env is committed (no secrets — safe defaults only).
# Put real credentials in .env.local (gitignored, never committed).
.env.local
.env.*.local
!.env
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
htmlcov/
*.resolved

# IDEs
.vscode/
.cursor/
.scripts/
.github/

requirements-ci.txt
# Local scripts (not tracked)
scripts/

# Specify / Speckit (local tooling & generated specs)
.specify/
specs/
# Local pytest / integration test workspace (entire tree)
test_reports/
pytest.ini
curl_smoke.sh 
# Project Specific
outputs/
# mediamtx binary & local config (download separately)
bin/
mediamtx.yml
mediamtx*.yml
# Runtime storage (created by FrameBus / task workers)
store/
# Test / QA artifacts
test_logs/
# Runtime test + pipeline output artifacts (videos, crops, JSONL events, logs)
artifacts/
# Local export trees (e.g. batch image+json dumps; not under outputs/)
output*/
videos/
logger/app.log
evidence/
torch*

# Local secrets / OS noise
*.pem
.DS_Store
Thumbs.db
*.tmp
*.temp
*.bak
*.swp
*.swo
*~

# Cashier frame batch tests / YOLO tooling (outputs/ already ignores test runs)
runs/
wandb/
*.engine

# YOLO / ONNX weights at repo root (if copied outside models/)
yolov8n.pt
yolov8n.onnx
yolov8n.yaml

# models/ — binaries not in git; README + zero-byte placeholders define expected paths on edge
models/*
!models/.gitkeep
!models/README.md
!models/yolov8n.pt
!models/best_aged_gender_6.onnx
!models/best_mood.onnx
!models/best_ppe.onnx
!models/best_cashier.onnx
!models/osnet_x1_0.pt
!models/image_encoder.onnx
!models/text_encoder.onnx

# Ad-hoc test scripts at repo root only (tracked pytest lives under tests/)
/test_*.py
/*_test.py
/test.py
test_results/
test_results.log
# Documentation (local reference — not versioned)
docs/
service_doc/
*.md
!/README.md
!/models/README.md

data/
.pytest_cache/
