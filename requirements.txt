# Requirements for NVIDIA Orin
# Note: For best performance on Jetson/Orin, you should install PyTorch and Torchvision
# using NVIDIA's provided wheels or use the l4t-ml Docker containers.
# Do NOT install torch/torchvision directly via pip from PyPI on Jetson.

# Core ML and Utils
ultralytics
onnx
onnxruntime-gpu
numpy<2.0.0
pandas
matplotlib
seaborn
psutil
tqdm

# GenderAge MobileNetV3 model
timm                   # MobileNetV3 backbone (required for gender_age_model.py)

# Utilities
humanize               # used in model_inference.py
tabulate               # optional: nicer tables in compare_models.py
