# Core
ultralytics>=8.0.0          # YOLOv8
opencv-python-headless      # OpenCV (headless for Docker/server)
fastapi                     # Web framework
pydantic                    # Data validation
uvicorn[standard]           # ASGI server
numpy<2                     # Numerical computing
psutil                      # System monitoring

# Config loading
python-dotenv>=1.0.0        # Reads .env file

# Utilities
requests>=2.28.0
Pillow>=9.0.0
PyYAML>=6.0
tqdm>=4.64.0
onnxruntime
imageio>=2.28.0          # GIF generation for evidence compilation

# Tests (optional)
pytest>=7.0.0
httpx>=0.24.0          # FastAPI TestClient / Starlette