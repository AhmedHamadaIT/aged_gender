# Core
ultralytics>=8.0.0          # YOLOv8
opencv-python-headless      # OpenCV (headless for Docker/server)

# Config loading
python-dotenv>=1.0.0        # Reads .env file

# Utilities
requests>=2.28.0
Pillow>=9.0.0
PyYAML>=6.0
tqdm>=4.64.0
onnxruntime
scipy>=1.10.0

#ReID
torchreid==0.2.5
qdrant-client>=1.7.0

# Live streaming
redis>=5.0.0

# BoT-SORT / Ultralytics tracker (native LAPJV; falls back to lap/ shim + SciPy if missing)
lap>=0.5.12

# Optional: download weights from Google Drive (tools/download_models.py)
gdown>=5.0.0

# Face Recognition
insightface>=0.7.3
faiss-cpu>=1.7.4
